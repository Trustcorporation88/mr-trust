{
  "version": 2,
  "trailingSlash": false,
  "buildCommand": "echo 'No build needed - API only deployment'",
  "outputDirectory": ".",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
