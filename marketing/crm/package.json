{
  "name": "meishop-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "test": "echo 'No tests specified'"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
