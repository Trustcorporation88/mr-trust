{
  "name": "meishop-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ./frontend && npm ci && npm run build"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
