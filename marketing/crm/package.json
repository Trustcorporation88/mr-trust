{
  "name": "meishop-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist dist"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
