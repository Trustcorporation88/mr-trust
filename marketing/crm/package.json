{
  "name": "meishop-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.21.0"
  }
}
