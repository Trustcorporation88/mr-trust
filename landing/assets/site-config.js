/**
 * Configuração do site — edite aqui antes de publicar.
 * Repositório: https://github.com/Trustcorporation88/mr-trust
 */
window.MR_TRUST_SITE = {
  /** E-mail de contato e destino do fallback mailto */
  contactEmail: "sales@mrtrust.com",

  /**
   * Formspree: crie em https://formspree.io → New Form → copie o ID (ex: "abcxyzde")
   * Deixe vazio ("") para usar apenas mailto até configurar.
   */
  formspreeFormId: "",

  /**
   * HubSpot (opcional): IDs do formulário embutido.
   * Se preenchidos, o script carrega o embed no lugar do formulário HTML.
   */
  hubspotPortalId: "",
  hubspotFormId: "",

  /** URL pública do site (GitHub Pages / Netlify / Vercel) */
  siteUrl: "https://trustcorporation88.github.io/mr-trust/",

  /**
   * Demo Streamlit — troque pela URL do Streamlit Cloud quando publicar.
   * Local: streamlit run web_app.py  →  http://localhost:8501
   */
  demoDashboardUrl: "http://localhost:8501",

  /** CRM Streamlit (opcional): streamlit run CRM/crm_app.py → porta 8513 */
  demoCrmUrl: "http://localhost:8513",
};
